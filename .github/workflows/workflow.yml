name: Python Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.x  # Substitua "3.x" pela versão desejada do Python

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt  # Substitua pelo caminho correto do seu arquivo de requisitos

    - name: Build
      run: |
        python setup.py sdist bdist_wheel

    - name: Display built files
      run: |
        ls dist
